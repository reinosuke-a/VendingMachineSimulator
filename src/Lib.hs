module Lib
    ( simulate
    , VendingState(..)
    , Input(..)
    , transition
    ) where  -- 必要な関数や型をエクスポートする

import VendingMachine (simulate, VendingState(..), Input(..), transition)  -- VendingMachine モジュールをインポート
